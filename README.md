# Self-Pruning-Neural-Network
A Self-Pruning Neural Network is a deep learning model that automatically removes unnecessary weights during training. It uses learnable gates and sparsity constraints to reduce model size, improve efficiency, and maintain accuracy, making it suitable for fast and resource limited AI applications.
